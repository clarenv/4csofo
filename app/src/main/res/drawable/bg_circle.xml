<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/white" /> <!-- Background color ng bilog -->
    <stroke
        android:width="2dp"
        android:color="@color/primary" /> <!-- Optional: border color -->
</shape>
